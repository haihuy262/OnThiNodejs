<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Bootstrap Example</title>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
  </head>
  <body>
    <div class="container mt-3 bg-body-tertiary">
      <h2 class="text-center">Sửa thông tin xe máy</h2>
      <form
        action="/api/edit/<%= productToEdit._id %>"
        method="post"
        enctype="multipart/form-data"
      >
        <div class="mb-3 mt-3">
          <input
            type="hidden"
            class="form-control"
            id="idObject"
            name="idObject"
            readonly
          />
        </div>
        <div class="mb-3 mt-3">
          <label for="tenXeMay">Tên xe máy:</label>
          <input
            type="text"
            class="form-control"
            id="tenXeMay"
            placeholder="Nhập tên xe"
            name="tenXeMay"
            value="<%= productToEdit.tenXeMay %>"
          />
        </div>
        <div class="mb-3">
          <label for="hangSanXuat">Hãng sản xuất:</label>
          <input
            type="text"
            class="form-control"
            id="hangSanXuat"
            placeholder="Nhập hãng sản xuất"
            name="hangSanXuat"
            value="<%= productToEdit.hangSanXuat %>"
          />
        </div>
        <div class="mb-3">
          <label for="editAnhXeMay">Ảnh xe máy:</label>
          <input
            type="file"
            class="form-control"
            id="editAnhXeMay"
            name="editAnhXeMay"
            value="<%= productToEdit.anhXeMay %>"
            multiple
          />
        </div>
        <div class="mb-3">
          <label>Ảnh hiện tại:</label>
          <% if (productToEdit.anhXeMay && productToEdit.anhXeMay.length > 0) {
          %> <% productToEdit.anhXeMay.forEach((image) => { %>
          <img
            src="/images/<%= image %>"
            alt="Ảnh sản phẩm"
            style="width: 50px; height: 50px"
          />
          <% }) %> <% } else { %>
          <p>Không có ảnh</p>
          <% } %>
        </div>
        <div class="mb-3">
          <label for="giaTien">Giá tiền:</label>
          <input
            type="number"
            class="form-control"
            id="giaTien"
            name="giaTien"
            placeholder="Nhập giá tiền"
            value="<%= productToEdit.giaTien %>"
          />
        </div>
        <button type="submit" class="btn btn-primary">Xác nhận</button>
      </form>
    </div>
  </body>
</html>
